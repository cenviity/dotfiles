# Generated by Git for Windows.
test -f ~/.profile && . ~/.profile
test -f ~/.bashrc && . ~/.bashrc

. "$HOME/.local/bin/env"

# Prioritise user VS Code binary.
PATH="~/AppData/Local/Programs/Microsoft\ VS\ Code/bin/:$PATH"

# Add Cargo binaries.
PATH="$HOME/.cargo/bin:$PATH"

# Add R binaries.
PATH="/c/Program\ Files/R/R-4.5.2/bin:$PATH"

# Add aliases.
source $HOME/.bash_aliases

# Add `br` shell function for `broot`.
source <(broot --print-shell-function bash)

# Add `chezmoi` shell completions.
source <(chezmoi completion bash)

# Add `jj` shell completions.
source <(COMPLETE=bash jj)

# Add `zoxide` shell completions.
eval "$(zoxide init bash)"
